# 智能会议助手——设计说明

## 目标与范围

本项目是可复现的中文会议助手演示：登录态已在 HTTP 边界模拟为 `X-Demo-Actor`，用户可创建、查询和修改单次会议。创建和修改必须经过“预览—明确确认—执行”；模型（或演示解析器）只解析文本，不能决定身份、权限或调用数据库。默认时区为 `Asia/Shanghai`。

本版不做语音转写、周期会议、真实 Google/Outlook OAuth、跨组织权限、删除、批量修改、RAG 或多 Agent。危险请求、任意 SQL 和任意 HTTP 请求都会被拒绝，且没有对应写工具。

## 技术与数据流

- Python **>=3.12**、FastAPI、Pydantic v2、LangGraph、SQLite 和 pytest。
- `Settings.mode` 仅接受 `demo` 或 `llm`，无效值在启动时立即失败。默认使用确定性的 `DemoInterpreter`，无需密钥；`llm` 模式使用配置的模型和可选兼容服务 base URL 构建 `LLMInterpreter`。
- API 密钥只从进程环境读取，不进入 `Settings`，也不记录到日志。缺少密钥时，`llm` 模式以受控配置错误停止启动。
- 两种解释器都只输出结构化 `MeetingCommand`。固定 LangGraph 流程仍负责身份、权限、校验、忙闲/会议室工具、预览确认和写入，模型没有工具或写权限。
- 推荐支持稳定结构化输出的小模型：这里仅做窄模式字段提取，更大推理模型会增加成本和延迟，却不会获得更多权限。
- `meetings.db` 持久化会议与乐观锁版本；启动时幂等写入一条 Alice 可见的种子会议。
- 日历忙闲和会议室搜索是进程内固定模拟，实现可重复的冲突检查和房间排序。

```mermaid
flowchart LR
  U[用户文本] --> P[安全预检]
  P --> I[文本解析]
  I --> V[字段、权限和业务校验]
  V --> Q[查询或忙闲/房间模拟]
  Q --> R[预览]
  R --> C[明确确认]
  C --> W[参数化 SQLite 写入]
```

会话状态按 `(conversation_id, actor_id)` 隔离，包含草稿、选中的会议、待确认动作和最近工具结果。它是 `MeetingAssistant` 进程内字典，不使用 `SqliteSaver` 或任何检查点数据库；服务重启会丢失未确认草稿和选中上下文，但不会丢失 SQLite 中已保存的会议。修改预览还绑定用户、动作、草稿哈希与会议版本，参数变化或并发版本变化会使确认失效。

## 安全与验收

`X-Demo-Actor` 只在服务端解析，查询只返回创建者或参会者可见的会议，修改只允许组织者。忙闲响应只含 busy/free 区间，不含他人会议标题。写入使用参数化 SQL、幂等键和乐观锁；审计范围为请求 ID、用户、动作、资源、权限结果和变更摘要，不能记录密钥或完整私人日历内容。

测试使用临时 SQLite、固定时钟和 FastAPI ASGI 客户端，覆盖时间、不可变状态、房间排序、权限、确认、冲突与四个端到端场景：创建、忙闲拒绝、查询后改期，以及危险请求零写入。详见 `README.md` 中的启动、重置、Docker 和演示命令。
