# 智能会议助手——设计说明

## 目标与范围

本项目是一个可复现的中文会议助手：用户可以创建、查询和修改单次会议，直接查询一名或多名参会人的空闲时间，并根据主题、人数、设备和时间推荐会议室。创建和修改必须经过“预览—明确确认—执行”。默认时区为 `Asia/Shanghai`。

本版不实现语音转写、周期会议、真实 Google/Outlook OAuth、跨组织权限、删除、批量修改、RAG 或多 Agent。危险请求、任意 SQL 和任意 HTTP 请求均会被拒绝，系统也没有对应写工具。

## 技术选型

- Python 3.12、FastAPI、Pydantic v2、LangGraph、httpx、SQLite 和 pytest；
- LangGraph 负责固定、可审计的工作流，避免模型进入无边界 ReAct 循环；
- 默认 `demo` 解释器保证无需密钥即可复现，`llm` 解释器使用模型的结构化输出能力；
- 推荐小型结构化输出模型，因为模型只做意图和字段提取，不负责权限、工具选择或写入。

## 数据流与状态

```mermaid
flowchart LR
  U["用户文本"] --> P["安全预检"]
  P --> I["结构化解释"]
  I --> V["字段与权限校验"]
  V --> T["受控会议工具"]
  T --> H["模拟 HTTP API"]
  H --> R["忙闲结果与会议室排序"]
  R --> C["预览和明确确认"]
  C --> D["参数化 SQLite 写入"]
```

模拟日历和会议室能力通过 FastAPI 路由公开。主运行时使用 `CalendarClient`、`RoomClient` 和 `httpx.ASGITransport` 发出 HTTP 请求，因此会经过序列化、路由、状态码检查和 Pydantic 响应校验，同时不需要额外服务进程。

会话状态按 `(conversation_id, actor_id)` 隔离，包括会议草稿、选中会议、可见候选会议、待确认动作和最近工具结果。多个会议时，系统返回序号并支持按序号、唯一主题或会议 ID 选择。只提供新时间而未提供日期时，以已选会议原日期为基准。

会话状态保存在进程内；重启会丢失未确认草稿和候选上下文，但不会丢失 SQLite 中已保存的会议。更新预览绑定用户、动作、草稿哈希、会议 ID 和版本，并通过乐观锁阻止过期确认。

## 空闲查询与会议室推荐

`availability` 命令包含参会人、查询窗口和期望时长。精确时间查询返回“均空闲”或明确的忙碌参会人；较宽时间窗口通过 `find_common_free_slots` 返回共同空闲区间。日历响应不包含会议标题。

创建和修改会议时同样会调用日历 API 检查冲突。存在冲突时返回明确业务提示；只有 HTTP、状态码或响应结构异常才返回“会议服务暂时不可用”。会议室 API 先过滤容量、设备和时间冲突，再根据主题匹配与容量接近度排序，预览使用最高排名会议室。

## 安全与持久化

`X-Demo-Actor` 只在 HTTP 边界解析。查询只返回组织者或参会者可见的会议，修改只允许组织者。模型无法指定登录身份，也无法直接调用数据库或任意网络。

写入使用参数化 SQL、幂等键和乐观锁；删除和批量写工具不存在。确认哈希绑定用户、动作和完整预览载荷，参数变化会使旧确认失效。API key 只从进程环境读取，不进入配置对象、日志或示例文件。

## 测试与交付

测试使用临时 SQLite、固定时钟和 FastAPI ASGI transport，覆盖自然语言解析、空闲查询、会议室排序、权限、确认、多会议选择、无日期改期、并发冲突、模拟 HTTP API、危险请求零写入和完整用户旅程。

setuptools 包数据包含静态页面和 `app/repositories/schema.sql`。验收包括 pytest 分支覆盖率、Ruff、compileall、依赖一致性、sdist/wheel 构建，以及 wheel 隔离安装后的数据库初始化。
